# ЛПР 3  Условные конструкции if else и switch

**Студент:** Хамидов Дамир  
**Группа:** 20 РПО

Я выполнил все задания лабораторно-практической работы. В программах я использовал только изученные конструкции `if`, `else if`, `else`, `switch`, базовые переменные, ввод и вывод. В каждом исходнике используется `using namespace std;`.

Все 14 программ я отдельно скомпилировал и проверил на контрольных данных. Ошибок компиляции и аварийного завершения нет. Для каждой программы приложен PNG-снимок полного исходного кода с отметкой о проверке.

## Состав папки

- `Исходники` — 14 отдельных программ на C++.
- `Скриншоты_кода` — 14 изображений с полным кодом программ.
- `ПРОВЕРКА.md` — перечень тестов и итог проверки.

## Итог

Я убедился, что условные операторы позволяют выполнять разные действия в зависимости от условия. `if` удобно использовать для сравнений и диапазонов, а `switch` — для выбора одного варианта по конкретному значению. Все подготовленные программы работают.
